# Compatibility Note: Qwen Image Generation

This file is retained for older workflows that still refer to the original `qwen` file name.

Use `references/maas-image-generation.md` as the primary reference. The workflow is Huawei Cloud ModelArts MaaS image generation; `qwen-image` is only the default MaaS model parameter.
